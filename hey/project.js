const plus = document.querySelector('.plus')
const add = document.querySelector('.add')
const sort = document.querySelector('.sorting')

plus.addEventListener('keyup', function(event){
  if (event.key == 'Enter')
  console.log(this.value)
}
)
